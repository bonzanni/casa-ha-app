schema_version: 1
model: "${PRIMARY_AGENT_MODEL}"
enabled: true
tools:
  allowed:
    - Read
    - Write
    - Edit
    - Bash
    - Skill
    - mcp__casa-framework__cancel_engagement
    - mcp__casa-framework__delegate_to_specialist
    - mcp__casa-framework__engage_executor
    - mcp__casa-framework__get_schedule
    - mcp__casa-framework__send_message
  disallowed: []
  permission_mode: acceptEdits
  max_turns: 20
mcp_server_names:
  - homeassistant
  - casa-framework
memory:
  token_budget: 4000
  read_strategy: per_turn
  scopes_owned: [personal, business, finance]
  scopes_readable: [personal, business, finance, house]
  default_scope: personal
channels:
  - telegram
  - webhook
session:
  strategy: persistent
  idle_timeout: 86400
cwd: /addon_configs/casa-agent/workspace

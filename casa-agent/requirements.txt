# claude-agent-sdk pin. Bumped 0.1.72 -> 0.2.87 (spec §6) for the v0.2.82
# stderr-callback exception-isolation fix (sdk_logging.with_stderr_callback
# relies on the SDK swallowing callback exceptions). The pip pin also pins
# the SDK-bundled CLI used by residents + the in_casa driver; the global npm
# claude CLI is pinned to the matching version in Dockerfile.
# G-1 ('NoneType has no attribute items' from hook_0..3 on Edit) is NOT an
# upstream fix: verified 0.2.87 still does hook_output.items() with no None
# guard (_internal/query.py:_convert_hook_output_for_cli, call site ~:452).
# It is mitigated Casa-side by H-2 (v0.36.1) — every hook callback returns
# {} / _deny(...), never None (hooks.py:187-192). So this bump neither fixes
# nor regresses G-1; Casa is immune across this range.
# Bumped 0.2.87 -> 0.2.114 (v0.59.1, Dependabot #104): no breaking changes
# across the range (release-notes reviewed); additive TaskUpdatedMessage typing
# (0.2.101) and a defensive mcp<2.0.0 pin (0.2.96, matches ours below).
claude-agent-sdk==0.2.114
python-telegram-bot==22.7
# Bumped 3.13.5 -> 3.14.1 (v0.59.1): clears 11 security advisories (deserialization,
# HTTP request-smuggling/pipelining, websocket memory-limit bypass, etc.), all
# fixed by <=3.14.1. Dependabot #97 (branch deleted before recreate).
aiohttp==3.14.1
apscheduler==3.11.2
pyyaml==6.0.3
jsonschema==4.26.0
opentelemetry-api>=1.43.0
# E-12 (v0.37.0): Channels MCP capability — used by casa_engagement_channel.
# Build-time probe (docs/superpowers/specs/2026-05-12-e12-claude_code-channels-A6-findings.md
# §A.6.1) confirmed FastMCP `instructions=` kwarg + `Server.create_initialization_options(
# experimental_capabilities={...})` bootstrap path on this version range.
mcp>=1.28.1,<2

[pytest]
asyncio_mode = auto
markers =
    unit: fast, dependency-free unit tests
    slow: tests that take a long time (excluded from default CI run)
    docker: requires Docker (skipped in pure-unit test runs)

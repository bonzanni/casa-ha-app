# ha-prod-console project config for casa-ha-app.
# Schema reference: ~/Projects/ha-prod-console/docs/project-config.md
# Template:         ~/Projects/ha-prod-console/example-ha-prod-console.yml

# ════════════════════════════════════════════════════════════════════
#  Group A — HA addon lifecycle (required)
# ════════════════════════════════════════════════════════════════════

host: n150-ha
addon_slug: c071ea9c_casa-agent
container: addon_c071ea9c_casa-agent

# The HA "SSH & Web Terminal" addon runs as unprivileged hassio (uid 1000).
# Docker-socket ops need passwordless sudo; prefix every remote command.
ssh_prefix: "sudo -n "

# ════════════════════════════════════════════════════════════════════
#  Group B — HMAC-signed app probes (webhook smoke/regression)
# ════════════════════════════════════════════════════════════════════

base_url: https://agent.oudekamp.bonzanni.casa

# Casa uses X-Webhook-Signature with a bare hex digest (no `sha256=` prefix),
# per casa_core.py _verify_webhook. Default is GitHub-style X-Hub-Signature-256.
hmac_header: X-Webhook-Signature
hmac_format: "${HEX}"

secret_source:
  type: ssh-docker-exec
  container: addon_c071ea9c_casa-agent
  command: cat /data/webhook_secret
  cache_ttl_minutes: 60

probes:
  healthz:
    method: GET
    path: /healthz
    expect_status: 200

  turn-assistant:
    method: POST
    path: /invoke/assistant
    sign: hmac
    body_json: { prompt: "say 'probe ok'" }
    expect_status: 200
    expect_body_contains: probe ok

  voice-sse:
    method: POST
    path: /api/converse
    sign: hmac
    sse: true
    body_json: { prompt: "say 'probe ok'", scope_id: "probe-scope" }
    expect_events: [block, done]

  # ── Negative surface audit ────────────────────────────────────────
  # Unsigned HMAC-gated endpoints must reject with 401/403 before any
  # bus dispatch. Regression gate: catches if anyone wires a new
  # handler to `/invoke/*` / `/webhook/*` / `/api/converse` without
  # running HMAC verify first.

  surface-invoke-unsigned:
    method: POST
    path: /invoke/assistant
    body_json: { prompt: "surface audit" }
    expect_status: 401
    expect_body_contains: invalid signature

  surface-webhook-unsigned:
    method: POST
    path: /webhook/surface-audit
    body_json: { probe: true }
    expect_status: 401
    expect_body_contains: invalid signature

  surface-voice-sse-unsigned:
    method: POST
    path: /api/converse
    body_json: { prompt: "surface audit", scope_id: "surface-audit" }
    expect_status: 401
    expect_body_contains: invalid signature

  surface-telegram-unsigned:
    method: POST
    path: /telegram/update
    body_json: { update_id: 0, message: { text: "surface audit" } }
    expect_status: 403

smoke: [healthz, turn-assistant, voice-sse]

regression:
  - healthz
  - turn-assistant
  - voice-sse
  - surface-invoke-unsigned
  - surface-webhook-unsigned
  - surface-voice-sse-unsigned
  - surface-telegram-unsigned

name: Alex
role: finance
description: >
  D-2 fixture — finance executor pre-flipped to enabled, with
  n8n-workflows stripped from mcp_server_names so the enable path is
  tested independently of n8n registration. Not a shipping default.
enabled: true
model: sonnet
personality: |
  Alex (D-2 fixture — test-only).
tools:
  allowed: [Read, Skill]
  disallowed: [Bash, Write, Edit]
  permission_mode: acceptEdits
  max_turns: 10
mcp_server_names:
  - casa-framework
memory:
  token_budget: 0
session:
  strategy: ephemeral
  idle_timeout: 0

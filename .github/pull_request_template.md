## What this changes

<!-- One or two sentences. What behaviour differs after this merges? -->

## Docs impact

<!-- Tick one. CI separately checks that any document CLAIMING a changed surface changed
     with it, so the box and the check are not the same thing: the box is the reasoning. -->

- [ ] Updated `docs/` — which files:
- [ ] User-facing only — updated `casa/DOCS.md`
- [ ] No docs change needed — because:

## Publication

<!-- This repository is public and every push is irreversible. -->

- [ ] Nothing here is internal-only, and no fact in it needs an operator, a production
      system or a private repository to verify — see `docs/doctrine/publishing.md`.
